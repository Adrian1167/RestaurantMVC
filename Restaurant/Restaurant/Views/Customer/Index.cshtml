@model  IEnumerable<Customer>


@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Customer</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Customer" asp-action="Create" class="btn btn-primary"> 
                Add Customer
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
           
            <th>
                Name
            </th>
            <th>
                Address
            </th>

            <th>
                Phone
            </th>

           
            <th></th>
        </tr>
    </thead>
    
    <tbody>
        @foreach(var obj in Model)
        {
            
            <tr>
                <td width="30%">
                    @obj.Name
                </td>
                <td width="30%">
                    @obj.Address
                </td>

                <td width="30%">
                    @obj.Phone
                </td>
            

                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Customer" asp-action="Edit" asp-route-id="@obj.CustomerId" class="btn btn-primary mx-2">Edit</a>
                        <a asp-controller="Customer" asp-action="Delete" asp-route-id="@obj.CustomerId" class="btn btn-primary mx-2">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
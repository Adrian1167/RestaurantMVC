@model  IEnumerable<Reservation>


@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Reservation</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Reservation" asp-action="Create" class="btn btn-primary"> 
                Add Reservation
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Date
            </th>
            <th>
                Number of persons
            </th>
           
            <th></th>
        </tr>
    </thead>
    
    <tbody>
        @foreach(var obj in Model)
        {
            
            <tr>
                

                <td width="50%">
                    @obj.Date
                </td>
                <td width="50%">
                    @obj.NOofPersons
                </td>


                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Reservation" asp-action="Edit" asp-route-id="@obj.ReservationId" class="btn btn-primary mx-2">Edit</a>
                        <a asp-controller="Reservation" asp-action="Delete" asp-route-id="@obj.ReservationId" class="btn btn-primary mx-2">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>